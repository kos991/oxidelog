name: goal

on:
  push:
    branches: ["main", "master", "feature/**"]
  pull_request:

jobs:
  local-goal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Run Linux local goal
        run: bash scripts/goal-linux.sh
